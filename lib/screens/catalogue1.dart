import 'package:flutter/material.dart';

class Catalogue1 extends StatelessWidget {
  const Catalogue1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
      body: const Center(
        child: Text('Catalogue1'),
      ),
    );
  }
}
