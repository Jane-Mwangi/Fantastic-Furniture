
import 'package:flutter/material.dart';

class Catalogue3 extends StatelessWidget {
  const Catalogue3({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      backgroundColor: Colors.pink,
      body:const Center(
        child: Text('Catalogue2'),
      ),

    );
  }
}
