
import 'package:flutter/material.dart';

class Catalogue2 extends StatelessWidget {
  const Catalogue2({super.key});
  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      backgroundColor: Colors.purple,
      body:const Center(
        child: Text('Catalogue2'),
      ),

    );
  }
}
